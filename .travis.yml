# https://travis-ci.org/#!/phlax/debian-criu

install:
  - make debs

script:
  - ls build
  - make install-test

deploy:
  provider: script
  script: make publish
  skip_cleanup: true
  on:
    all_branches: true
    condition: $TRAVIS_BRANCH =~ ^(master)$
